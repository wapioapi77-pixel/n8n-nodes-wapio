# Publishing identity

- Push only to repositories owned by `wapioapi77-pixel`. Before pushing, verify that `origin` uses that GitHub owner.
- Keep this repository's local Git identity as `Wapio <support@wapio.io>`.
- Do not add an individual's name, email address, profile link, or account identity to commits, release notes, repository metadata, or documentation unless the user explicitly requests it.
